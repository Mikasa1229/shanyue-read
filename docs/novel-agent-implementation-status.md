# Novel Agent Implementation Status

Last verified: 2026-08-01

## Newly completed in this pass

- The launcher-standard Agent process on `8086` was validated with matching non-empty Gateway/Agent secrets; `scripts/validate-agent-deployment.ps1` passed with health `UP`, Flyway V26--V30 successful, and direct browser-facing Agent access returning `404`.
- The 《剑来》 source ledger is complete (`1279/1279 READY`), all Agent index jobs are `COMPLETED`, and the RabbitMQ index/delete queues and DLQs are empty. Three stale duplicate-node DLQ messages were acknowledged only after their durable jobs were verified completed; graph inserts now use PostgreSQL `ON CONFLICT DO NOTHING` to make concurrent chapter delivery idempotent.
- Embedding now has a real OpenAI-compatible provider adapter (`AGENT_EMBEDDING_PROVIDER=openai-compatible`, `/v1/embeddings`) with batched requests, configured dimensions, timeout/failure cooldown, Prometheus outcomes, and deterministic hash fallback. The default remains key-free development mode; changing provider, dimension, or model version requires a versioned Milvus collection and re-projection. Contract tests cover provider ordering, dimension rejection, and cooldown fallback.
- Authenticated infrastructure metadata now exposes the configured embedding provider/model/dimension alongside the embedding version, so operators can verify that a Milvus collection and re-projection job match the active semantic model.
- Full graph rebuilds now cache nodes/edges per rebuild and project Neo4j with bounded `UNWIND` batches. Optional evidence stores can be repaired through `POST /internal/agent/knowledge/books/{canonicalBookId}/evidence:reproject` without re-extracting graph claims.
- Milvus now uses explicitly versioned 256-dimension collections (`*_vectors_256`) matching `HashEmbeddingService`; a live reproject of an indexed chunk succeeded with no dimension error. The previous incompatible 1536-dimension collection is not reused.
- The authoritative PostgreSQL graph was reprojected to Neo4j with the new bounded `CREATE` batch path: both stores now contain `26893` nodes and `399953` edges for the indexed work. A `NovelEntity.id` Neo4j index is created automatically for future projections, so this repair does not depend on a manual database command.
- A real DeepSeek platform request completed through Gateway login, credit/session creation, LightRAG retrieval, and SSE. When DeepSeek returned an empty usage object, the Agent now recorded non-zero prompt/output estimates with `token_usage_source=ESTIMATED` instead of mislabeling them as provider usage, while bounded `community_tokens`/`evidence_tokens` remained non-zero. The response emitted `recommendations`, `graph`, and `done` events. The MCP verifier passed initialization, lifecycle, schema, allowlist, and server-side spoiler-boundary checks against the deployed standard Agent. An independent `@modelcontextprotocol/sdk@1.12.1` Streamable HTTP client also discovered all five read-only tools and completed `book.search`; its report contains only protocol/tool counts.
- `scripts/validate-agent-e2e.ps1` now provides a repeatable secret-safe login/session/SSE smoke test, and `scripts/check-status.ps1` probes the stable unauthenticated `/api/auth` route so a healthy Gateway no longer reports a false failure on a method-mismatch endpoint.

- The answer path is now LightRAG by construction rather than a GraphRAG-style mixed community search: it resolves visible entity seeds, queries only their bounded one/two-hop local graph, and selects `CHAPTER`/`GRAPH` cards first. Only when both local graph and low-level cards are absent can it escalate to `ARC`; `BOOK` and `BOOK_SAFE` cards are excluded from novel-QA context. After LightRAG establishes the work and reading boundary, Milvus, Elasticsearch, and PostgreSQL provide bounded original-text evidence candidates and the optional reranker orders those candidates. A shared prompt budget bounds all contributors and persists privacy-safe system/history/graph/community/evidence/tool token counts.

- The admin API and Agent control room now expose `GET /api/agent/admin/usage-breakdown?days=7`, a privacy-safe LightRAG budget view. It aggregates system/history/graph/community/evidence/tool tokens and shows at most 50 redacted recent rows without prompt text, user IDs, session IDs, request IDs or provider secrets, making token-efficiency evidence reproducible without weakening data minimization.
- `scripts/validate-agent-lightrag-budget.ps1` now verifies that the administrator response contains all six section counters and never exposes prompt, user/session/request IDs or key fields; it emits only aggregate counts and a timestamp.
- `AgentAdminControllerTest` covers the administrator endpoint's 90-day clamp, six-section aggregation and redaction contract; the latest full reactor reports `80` Agent tests with no failures.

- `.github/workflows/novel-agent-quality.yml` now fixes the release baseline in CI: JDK 17/Maven runs the complete backend reactor, Node 20 runs the locked frontend build, Docker Compose is parsed, and tracked files are checked for committed local provider/Gateway secrets. The workflow has no provider credentials and does not replace the external Reranker gate.

- The backend parent POM now pins `maven-compiler-plugin` 3.13.0 and `maven-surefire-plugin` 3.2.5. This removes Maven's implicit-plugin-version warning and makes compilation and test execution reproducible across local and CI Maven upgrades.

- Character interviews now resolve an entered alias only when it maps to exactly one visible, approved, confidence-qualified character. Ambiguous aliases fail closed and the response explicitly directs the reader to select a visible relationship-graph character, rather than role-playing the wrong person.

- The final local regression passes all nine backend modules, the Agent evidence/reranker tests, the frontend production build, Docker Compose configuration validation, and `git diff --check`. The only remaining plan acceptance items require a freshly deployed process or external providers/corpora and remain listed below rather than being represented as locally completed.

- `scripts/validate-agent-reranker-load.ps1` now makes the real-provider load gate executable through the normal Gateway session/SSE route. It aggregates 50-request latency, errors, and Prometheus reranker `success`/`fallback`/`disabled` deltas without printing access tokens, provider keys, or novel text; its PowerShell syntax has been validated.
- The in-process Reranker fixture additionally completes 50 HTTP-provider calls and asserts 50 successful metrics outcomes with bounded local latency; it deliberately remains labeled as adapter coverage rather than third-party provider evidence.
- Reranker response parsing now fails closed on malformed, out-of-range, or duplicate provider indexes instead of emitting duplicate evidence; the deterministic LightRAG-local order remains the bounded fallback.
- The final post-change Agent regression runs `80` tests; the standard `8086` deployment still passes health, Gateway boundary, authenticated SSE, corpus, and MCP checks. The only unresolved acceptance evidence is the real third-party Reranker provider run.

- Historical fixture validation: the copyright-cleared ten-chapter fixture completed `10/10` chapters and produced `24` nodes, `27` edges, one legacy deterministic clue, and `20` LightRAG community cards. That run describes the former review policy. Current user-triggered builds publish only model claims that pass verbatim-evidence, node/edge type, relation compatibility, confidence, and trusted-alias gates; visible regex/rule graph generation has been removed.

- The real DeepSeek answer-evidence gate has now run five original-fixture cases (citation, graph, clue, spoiler refusal, and tool-security) through the Gateway, and the server persisted evaluation run `358909459994513408` as `5/5 PASSED`. The temporary evaluation users, sessions, messages, and fixture projections were removed after the run; only the aggregate evaluation record remains.

- The internal MCP HTTP transport now accepts the standard post-initialization `notifications/initialized` notification and `ping`, returning no response for a notification as JSON-RPC requires. The repeatable verifier and the independent third-party SDK check both exercise this lifecycle before tool discovery; future SDK upgrades should rerun both checks.

- The official `@modelcontextprotocol/sdk` Streamable HTTP client was exercised against temporary isolated Agent and Novel processes: it negotiated `2024-11-05`, completed the standard lifecycle, discovered all five read-only tools, and completed cross-service `book.search`. This run exposed and fixed the shared Feign envelope's missing Jackson creator; the processes were removed afterward without altering the existing runtime.

- The compact floating Agent preview now degrades each optional recommendation, graph, and capsule request independently. A failure in one insight no longer hides the other safe results; it shows an explicit partial-degradation notice while keeping chat and reading available.

- LightRAG persists evidence-bearing `CHAPTER` summaries and bounded `ARC` cards. Full-book cards are not created for the QA path, and `BOOK_SAFE` catalogue material is excluded from graph context. Plot capsules preferentially use chapter summaries under the server-clamped reading boundary; a bounded chunk fallback is retained only while a newly indexed work has not yet produced its LightRAG cards.

- Agent-center initialization now separates essential conversation/model/credit/preference data from infrastructure, recommendation, reading-plan, and shelf-view enhancements. A failed optional endpoint leaves the usable surfaces available and names the degraded capability; insight cards likewise return independently instead of suppressing every reading-safe result after one dependency failure.
- Book-deletion processing is now terminally idempotent across RabbitMQ redelivery: a completed `BOOK_DELETE` ledger entry is neither reopened nor executed again. Focused regression tests cover deletion-ledger creation, completed-message skipping, private shelf ownership rejection, private shelf projection fail-closed behavior, and manual override reset.
- Discovery recommendations and similar-book DNA now discard orphaned indexed vector profiles that cannot resolve to a canonical readable source. The Agent center exposes a verified-reader jump for the remaining DNA cards, aligning recommendations, similar works, and citations with the same source-traceability invariant.
- Agent request protection now adds a gateway-observed client-address window and one in-flight generation slot per persisted conversation, in addition to the existing user, credit-budget, and circuit-breaker controls. Caller-supplied source-address headers are removed at the Gateway boundary; each stream consumes its user and network window exactly once, while Redis loss deliberately preserves the established availability-first fallback.
- Browser-facing Agent endpoints now require a separate Gateway-to-Agent forwarding secret. The Gateway strips any caller-supplied copy and injects the shared value with the trusted user/IP headers; direct requests to the Agent service port return `404`, so exposed development service ports cannot impersonate another user.
- The local backend launcher generates the Gateway-to-Agent secret only in its own child-process environment when absent, keeping development startup usable without writing any secret to a file; deployments must supply and rotate it explicitly.
- Embedding provenance is now a configured version rather than a hard-coded implementation label. Profile and LightRAG-community refreshes compare that version as part of their cache key, so a provider/version change re-projects unchanged content; the read-only infrastructure response reports the active embedding and reranker models for operator verification.
- Chapter indexing now uses that configured embedding version in its durable job fingerprint and persists it on both documents and chunks. A version bump therefore schedules unchanged chapters again and removes their prior Milvus chunk projection before rebuilding, preventing mixed-generation retrieval.
- LightRAG graph nodes, edges, and clues now retain extractor provenance and review state. Reader-facing graph, map, local-Neo4j expansion, and community construction require `APPROVED` evidence at the configurable graph-confidence threshold, keeping lower-confidence claims available for review without allowing them into answer context.
- The Agent admin console has a graph-claim review queue. Evidence-gated claims from an explicit user build begin as `APPROVED` so the completed insight is usable; administrators can move nodes, relations, and clues to `PENDING` or `REJECTED` within the selected canonical work. Review decisions synchronize to Neo4j and the LightRAG community projection, while non-approved clues remain excluded from spoiler-safe reader results.
- Historical shelf and favorite rows now acquire a canonical work ID lazily from their stored source metadata when users read those lists; new writes resolve the ID on the server rather than trusting a client-supplied work ID. Canonical merges cascade personal records to the retained work, while orphaned works clear those references instead of leaving stale Agent targets.
- Chapter retrieval now persists a SHA-256 content-version ledger at the novel source of truth before it publishes an Agent indexing event. Identical ready content is not requeued; Agent completion or failure writes `READY` or `FAILED` back to that ledger, and a later reader fetch safely retries a failed immutable version without making source reading depend on the asynchronous projection.
- The canonical-work and source-version reliability paths now have focused regression coverage: source-side SHA-256 registration, ready-version deduplication, failed-version replay, Agent `READY`/`FAILED` callbacks, callback degradation, approved merge redirection, orphan cleanup, and an explicit `404` for a missing version-ledger callback. The Agent treats a non-success callback body as an observable source-ledger rejection while preserving the completed durable index, so a bookkeeping outage is neither silently accepted nor retried as duplicate work.
- Character-interview admission now uses the same `APPROVED` and configurable graph-confidence gate as reader-facing graphs, maps, and clues; pending model-extracted entities cannot be selected merely because their name appears before the reader's chapter boundary.
- Chapter knowledge construction now prefers paragraph and sentence boundaries over fixed-width character cuts, retaining a bounded 120-character overlap (about 15% of the target size). An overlong individual sentence is safely split only as a last resort, preserving the plan's semantic-chunking intent without allowing unbounded records.
- Graph review gates now apply consistently to Neo4j local expansion and character/event profile vectors, not only relational graph reads. A review decision refreshes profiles before rebuilding LightRAG, so rejected claims are tombstoned from vector projections and newly approved claims become eligible without re-crawling text.
- The request guard now refuses whole-book and whole-chapter text requests before retrieval or model invocation. Every model prompt also carries an explicit short-quotation/summarize-only copyright rule, while citations stay bounded excerpts.
- Agent-center capsule entries, graph nodes and edges, clue records, and reading-map events now expose evidence jump-backs through the verified canonical reader link. Unavailable source mappings fail with a local explanation rather than fabricating a destination.
- Removing the last source mapping now deletes the source-side chapter-version ledger before its canonical work, respecting PostgreSQL's foreign key while the existing deletion event clears Agent projections. This prevents an orphan cleanup from failing midway and leaving a supposedly deleted work readable.
- Platform-owned model costs resolve from a persistent, versioned `provider + model` price table. Environment prices remain a safe compatibility fallback only when no enabled table entry exists.
- BYOK requests always store zero platform cost; the platform does not represent a user's provider bill as its own cost.
- The Agent admin page can view and update platform model prices in micro-CNY per thousand input/output tokens.
- MCP regression coverage now rejects non-allowlisted tools, invalid book/chapter values, and calls without a requesting user identity.
- Session metadata uses a database version column and optimistic locking. A concurrent completion retries only the metadata update, so it never repeats an LLM request, writes duplicate messages, or double-settles credits.
- Users can now delete one owned conversation (and its persisted messages) from the Agent center without having to erase all Agent data.
- A configurable nightly retention job expires inactive conversations in bounded batches; set `AGENT_CONVERSATION_RETENTION_DAYS=0` to opt out of automatic expiry.
- Model calls emit privacy-safe OpenTelemetry spans with only provider and mode tags. Local Docker now includes an OTLP Collector and Jaeger; tracing remains disabled by default until `AGENT_TRACE_SAMPLE_PROBABILITY` is raised.
- LightRAG community profiles are projected to Milvus when enabled, while PostgreSQL embeddings remain the authoritative fallback.
- Reading-map causal links and clue-to-event links require bounded, evidence-backed relations rather than treating a single shared character name as causality.
- Graph nodes now persist an identity key. A model extractor can supply an evidence-backed `identityHint` for same-name entities; ambiguous aliases remain unresolved instead of being silently attached to an arbitrary character.
- The Cohere-compatible reranker adapter has an in-process HTTP contract test covering bearer authentication, request payload, and provider result ordering. MCP discovery now includes JSON-RPC 2.0 validation and required JSON Schema arguments.
- Spoiler-sensitive APIs and tools no longer trust a client-supplied chapter alone. They clamp it against a private reader-novel progress projection and fail closed to chapter zero when that projection cannot be verified.
- Index and book-deletion consumers now have separate retry and dead-letter routing keys, so an unrecoverable projection deletion is observable and actionable without being mixed into chapter-index failures.
- The local status check now includes Prometheus, Jaeger, and the OTLP Collector, matching the Docker observability topology.
- Spring AI OpenAI auto-configuration is disabled because Agent constructs OpenAI-compatible clients per request for BYOK/platform routing; an absent platform key no longer prevents an otherwise valid Agent instance from starting.
- A real local Agent process now starts with PostgreSQL, Redis, RabbitMQ, Nacos, Elasticsearch, Milvus, and Neo4j enabled. `/actuator/health` returned `UP`, `/actuator/prometheus` was exposed, and an internally authorized chapter-index request reached Elasticsearch and initialized Milvus-backed profile/community projections without projection errors.
- Milvus collection initialization is explicit before Spring AI 0.8 receives a store. This works around the current Milvus server behavior where `DescribeIndex` throws for a missing first index, which otherwise prevented first-use vector collections from being projected.
- The Milvus first-use initialization path now has a regression test for both the missing-index exception and an existing index, so the runtime compatibility fix is no longer source-only behavior.
- MCP tool discovery and execution now share one internal tool schema. Required fields, primitive types, and `additionalProperties: false` are enforced before a read-only tool can run; tests cover missing and unexpected arguments.
- Removed stale Agent-center placeholder text referring to GraphRAG. The implemented graph context path is LightRAG.
- The floating Agent entry now renders returned chapter citations, hands its current persisted session to `/agent`, and uses a touch-draggable mobile bottom sheet rather than a reduced fixed card.
- Optional model-assisted graph extraction now has a server-side evidence gate: every entity, relation, alias, and same-name `identityHint` must be traceable to the indexed chapter text before it can be persisted. This prevents a model-only assertion from contaminating the LightRAG graph.
- `scripts/validate-agent-mcp.ps1` now makes the repeatable portion of deployed MCP validation executable: protocol initialization, tool schemas, missing-argument and write-tool rejection, plus optional server-side graph-boundary checks. It requires a runtime secret and deliberately never writes it to output.
- Submitted real-model evaluation evidence is now provenance-gated: citations must identify an indexed `canonicalBookId` and existing chapter chunks within the supplied boundary, while recommendation IDs must resolve to indexed canonical works. A forged but in-range number can no longer make a release gate pass.
- Reranker operational metrics now distinguish `success`, `fallback`, and deliberately `disabled` calls via `reader_agent_reranker_requests_total`, allowing the planned provider load test to measure degraded retrieval rather than infer it from logs.
- Recommendation-evaluation provenance now checks `reader-novel`'s canonical-work API rather than Agent's local index projection. This preserves the architecture's source-of-truth boundary and permits valid discovery candidates before on-demand indexing occurs.
- The bookshelf-to-reader route now preserves both `canonicalBookId` and the saved chapter index, so the floating Agent keeps the real work identity and spoiler boundary on this high-frequency reading path.
- The Agent center now makes saved encrypted BYOK configurations operational: users select platform credits or a specific saved model per conversation request; new saves select themselves, and deleting the selected configuration safely falls back to the platform mode.
- BYOK configuration now persists an OpenAI-compatible base URL and routes both regular and streaming calls through that selected endpoint. DeepSeek and OpenAI have provider-appropriate defaults, while a validated HTTP(S) custom endpoint supports compatible self-hosted providers.
- Existing BYOK records created before the base-URL migration remain usable: blank historical values resolve to the provider's safe default, while non-HTTP(S) endpoints are rejected. This behavior has a dedicated regression test.
- Book-insight fields now prefill from an Agent session's preserved reading context or the floating-entry route query, so graph, capsule, clue, timeline, and DNA views naturally follow the current work instead of requiring repeated manual IDs.
- The reader now uses only server-verified, visibility-aware session heartbeats for ranking and reading-credit eligibility. The old client-controlled duration endpoint is removed, and the daily qualified-time counter atomically clamps before it is recorded.
- As a reader scrolls across preloaded chapters, persisted reading progress and the route context used by the floating Agent advance together. Citation jump-backs preserve the canonical work ID, keeping Agent requests on their server-validated spoiler boundary.
- Standard chat in the Agent center now also carries the selected or restored book-and-chapter context, so ordinary follow-up questions use the same LightRAG and server-clamped spoiler boundary as character interviews instead of silently falling back to generic chat.
- BYOK endpoint validation now permits only plain HTTPS URLs on the official OpenAI/DeepSeek hosts or an explicitly configured review allowlist. HTTP, loopback/private IP endpoints, URL credentials, queries, fragments, and unapproved hosts are rejected before a user key can be sent.
- The RBAC-protected admin API now exposes graph rebuild for a canonical work. It rebuilds LightRAG/graph projections only from durable indexed chunks, without crawling a book source or accepting raw novel text.
- The Agent center now includes a deterministic, spoiler-safe reading plan sourced solely from the private shelf/progress projection. It prioritizes up to three recent works and never sends shelf text to a model or infers unread content.
- The floating entry now lets unauthenticated visitors inspect the Agent's capability and trial-credit explanation before choosing to log in; all session creation and chat calls remain login-protected.
- The floating entry now also exposes one to three safe shelf recommendations and, when the current route carries a canonical work, a reading-boundary-clamped first-degree graph preview. Both use the existing protected APIs and fail independently with an explicit non-blocking degradation message.
- Floating recommendation cards now resolve their canonical work through the protected reader-link API and open only a verified readable source. Missing or unavailable sources produce a local explanation instead of a fabricated destination.
- Agent SSE now sends protocol-level `recommendations` and reading-boundary-clamped `graph` events after the model reply, alongside `meta`, `tool_status`, `delta`, `done`, and `error`; both Agent surfaces consume recommendations without parsing model prose.
- The Agent center reading map now renders every visible indexed event as a chapter-ordered, branch-filterable map with evidence, confidence, and only in-range causal-link counts instead of truncating the feature to five summary bullets.
- Gateway authentication now strips any caller-supplied `X-User-Id` before injecting the single Sa-Token-derived identity. A regression test covers a spoofed Agent request header, closing a cross-user session, BYOK, credit, and preference isolation risk.
- The reading-context floating Agent now includes a collapsible spoiler-safe recap capsule. It is populated only by the existing server-clamped capsule API; where no indexed recap exists, it displays only the verified reading position rather than inventing a plot summary.
- Dynamic shelf management now provides persisted per-user shelf views (`FOLLOWING`, short session, weekend immersion, restart, and cleanup) with an explicit Automatic reset. The Agent derives unpinned defaults only from the user's bounded shelf projection; group changes do not move or delete real bookshelf entries.
- Saving a fixed shelf view now rechecks that the canonical work belongs to the requesting user's private shelf; an unavailable shelf projection fails closed, preventing arbitrary per-user metadata writes.
- Character-interview prompts now enforce visible `原文事实` / `基于事实的推断` / `不足以判断` sections, and the Agent center recognizes a conforming response. This makes the plan's fact-versus-inference distinction observable instead of leaving it as an implicit model instruction.
- Book-projection deletion now creates a persistent `BOOK_DELETE` job in the same operations ledger as chapter indexing. Delete failures survive RabbitMQ retry/dead-letter handling as visible failed jobs, and the existing admin retry action republishes them to the dedicated delete route rather than incorrectly sending them to the chapter-index consumer.
- The floating Agent now makes its chapter citations actionable as well: it resolves the cited canonical work through the protected reader-link projection, opens only a verified source/chapter, and reports unavailable mappings without changing reading state.
- Popularity fallback recommendations now carry a canonical work ID from the private source mapping. Agent recommendation assembly rejects unresolvable popularity rows, deduplicates all recommendation sources by canonical work, and applies completion/dismissal/shelf filters before returning cards, so every returned card remains traceable and actionable.
- Saved BYOK configurations can now be tested, disabled, re-enabled, replaced, and deleted from the Agent center. The test uses a rate-limited, one-token independent probe against the same reviewed endpoint and never creates a conversation, persists a message, or consumes platform credits; disabled models disappear from the chat selector and are still rejected server-side.
- The Agent admin console now exposes the real-model answer-evidence release gate as a JSON batch submission, alongside the deterministic policy suite. It does not trust the submitted result: the existing server-side evaluator validates canonical works, indexed citation boundaries, recommendation provenance, and tool/write assertions before retaining the run and per-case evidence.

## Verified

- Current full-reactor regression after the durable chapter/arc/book summary and stale-community projection fixes: `mvn test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'` passes all nine backend modules; `npm run build` in `frontend`, `docker compose -f docker-compose.yml config --quiet` in `docker`, and `git diff --check` all pass. The Agent module runs 74 tests.
- Latest full backend regression: `mvn test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'` passes all nine modules (150 tests); the latest focused Agent regression runs 74 tests after optional-vector/graph availability, traceability, rate-control, Gateway trust-boundary, alias-disambiguation, MCP lifecycle, LightRAG BOOK-card exclusion, and Reranker timeout/cooldown hardening.
- `npm run build` in `frontend` passes after the Agent-center degradation and actionable similar-book updates. `scripts/start-backend.ps1` passes PowerShell parser validation, and the repository secret scan found no hard-coded provider or Gateway credentials.
- `mvn -pl reader-gateway,reader-agent -am test '-Dtest=AuthGlobalFilterTest,AgentGatewayAccessFilterTest' '-Dsurefire.failIfNoSpecifiedTests=false'`: browser-facing Agent APIs reject direct service-port calls and Gateway forwarding/header sanitization passes (12 tests).
- `mvn -pl reader-agent -am test '-Dtest=ProfileVectorServiceImplTest' '-Dsurefire.failIfNoSpecifiedTests=false'`: an unchanged profile is re-embedded and re-projected when its embedding version changes.
- `mvn -pl reader-agent -am test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'`: 48 Agent regressions pass after durable chapter embedding-version reindexing and graph-provenance/review gating.
- `mvn test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'` in `backend`: all nine backend modules pass after the latest RAG and LightRAG provenance changes; `npm run build` in `frontend` also passes.
- `mvn -pl reader-novel,reader-agent -am test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'`: canonical-history migration and source-side chapter-version ledger wiring compile and pass with the Novel and Agent suites.
- `mvn -pl reader-gateway,reader-agent -am test '-Dtest=AgentRateLimiterTest,AuthGlobalFilterTest' '-Dsurefire.failIfNoSpecifiedTests=false'`: Gateway identity/source-header sanitization and Agent user/IP/session concurrency guards pass (14 tests).
- `mvn -pl reader-agent -am test '-Dtest=ShelfGroupServiceImplTest,KnowledgeIndexJobServiceImplTest,KnowledgeDeleteListenerTest' '-Dsurefire.failIfNoSpecifiedTests=false'`: six focused regression tests pass for shelf-view ownership/fail-closed behavior and deletion-ledger idempotency.
- `mvn test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'` in `backend`: the full nine-module Maven reactor passes, including gateway, user, novel, comment, checkin, and Agent services.
- Current full-reactor regression after reading-session and context propagation hardening: 93 tests pass across all nine backend modules, including 13 in `reader-novel` and 34 in `reader-agent`.
- Current full-reactor regression after adding the reading-plan endpoint and guest-entry preview: all nine Maven modules pass; `reader-agent` runs 34 tests.
- `mvn -pl reader-novel,reader-agent -am test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'`: reader-novel 11 tests and reader-agent 21 tests pass.
- `npm run build` in `frontend`: production build passes.
- `GET http://localhost:8086/actuator/health`: returned `UP`; the Prometheus endpoint also exposed `jvm_info` after the latest UI pass.
- `mvn -pl reader-agent -am test '-Dtest=*Test' '-Dsurefire.failIfNoSpecifiedTests=false'`: 27 Agent tests pass, including the graph-evidence and same-name identity-hint regression cases.
- The subsequent Agent regression run passes 28 tests, including rejection of non-resolving citation and recommendation evaluation evidence; the full nine-module Maven reactor also passes.
- PowerShell parser validation passes for `scripts/validate-agent-mcp.ps1`. Running it without a secret intentionally fails before any request, confirming its secret precondition.
- `docker compose -f docker-compose.yml config --quiet` in `docker`: configuration passes.

## Validation Environment Note

- The local Agent PostgreSQL schema has been migrated through Flyway V30 and verified for BYOK base URLs, shelf groups, embedding provenance, and graph review/provenance columns. The live Agent health endpoint remained `UP` afterward. Its Elasticsearch single-node Agent index was configured with zero replicas, yielding a green development cluster; code now creates fresh Agent indexes with the same single-node-safe setting.
- `scripts/validate-agent-deployment.ps1` makes the final deployed-process boundary check repeatable without exposing a secret: it requires health `UP`, Flyway V26--V30, and a `404` response from a direct browser-facing Agent API request. The standard `8086` process passed all three checks.
- Gateway-bound Agent endpoints are fail-closed in the source implementation: the access-filter regression suite covers both a missing caller header and an absent deployment secret, and both return `404` rather than exposing an API directly.
- Docker runtime verification was completed locally: PostgreSQL, Redis, RabbitMQ, Nacos, Elasticsearch, MinIO, Milvus, Neo4j, Prometheus, Jaeger, and the OTLP Collector are running; Elasticsearch returned a green cluster and Milvus/Neo4j passed their container-native health probes.
- The Docker engine initially exposed two invalid probes: the Milvus image has no `wget`, and an HTTP page-content check was not a reliable Neo4j readiness signal. Compose now uses Milvus `curl /healthz` and Neo4j `cypher-shell RETURN 1`, both verified healthy after recreation.
- A local Agent executable package starts successfully with test-only runtime environment variables and all optional data stores enabled. Its health and Prometheus endpoints were queried successfully. The smoke index exercised Elasticsearch and Milvus projections; graph extraction requires corpus text that matches its evidence rules, so graph semantic quality remains covered by the fixture validation runbook.

## Still intentionally open (optional external evidence)

- Optional external validation may add a configured third-party Reranker and an authorized answer-quality benchmark; neither blocks the local core. The copyright-cleared `original-synthetic` fixture has passed deterministic and real DeepSeek graph/runtime indexing, same-name identity separation, bounded LightRAG context, and standard-port E2E smoke. Local Milvus and Neo4j pause injections now keep the Gateway SSE path complete; the new Milvus timeout/cooldown path records `reader_agent_vector_recall_total{outcome="fallback"}` and falls back to PostgreSQL/Elasticsearch. Local adapter fixtures cannot replace provider/authorized-corpus evidence. The exact remaining external procedure is in `docs/novel-agent-external-validation-runbook.md`.
- The latest deployed Agent build also applies the Reranker operation timeout and failure cooldown: provider failures fall back to deterministic local ordering and do not repeat a timed-out call during the cooldown window. `ConfiguredRerankerServiceTest` covers the bounded failure and recovery behavior; `LightRagServiceImplTest` also proves that local/arc retrieval never admits `BOOK` or `BOOK_SAFE` cards. A real provider endpoint remains intentionally unconfigured.
- `scripts/validate-agent-external-readiness.ps1` now provides a secret-safe machine-readable preflight: it checks Agent health, the Gateway `404` boundary, the UTF-8 original fixture license/chapter count, and (when configured) the real Reranker HTTP response. With no provider configured it exits with `local_ready_external_optional`; a configured provider is probed only as optional evidence.
- `.env.example` now documents the optional OpenAI-compatible Reranker switch, endpoint, model, timeout and failure-cooldown variables; empty credentials keep the deterministic local fallback enabled without exposing a provider secret.
- Reranker now participates in the persisted `t_agent_model_route` gray-release path under the `RERANKER` key. Interactive retrieval passes a stable user cohort to the route resolver, and the admin console can publish the Reranker model and rollout percentage without changing provider secrets; the selected model is sent only in the bounded provider request.
- The post-route full regression is now `151` tests across nine modules (`75` in Agent); the rebuilt standard `8086` process passed deployment and authenticated SSE verification.
- Infrastructure version reporting now exposes both the configured and cohort-selected Reranker model. A clean rebuild fixed the Java `Map.of` arity limit for the expanded metadata payload, and an authenticated Gateway smoke returns HTTP `200` with the active embedding/Reranker versions.
- `scripts/validate-novel-corpus.ps1` now provides a secret-free, repeatable corpus gate: it verifies canonical source chapters are contiguous and all `READY`, then matches Agent documents, indexed chapters, completed jobs and chunk projections without printing novel content.
- The original `original-synthetic` fixture is the required copyright-cleared quality baseline; its real DeepSeek graph/runtime and five-case answer gate are complete. Additional licensed novel corpora remain optional quality expansion, not an unimplemented product capability.
- LightRAG callers now pass the plan's explicit 36-edge local-neighborhood budget (the Neo4j store still clamps any caller to 36), covering both normal answer retrieval and read-only graph tools; the rebuilt `8086` Agent contains this correction.
- The MCP `knowledge_graph.query` response is also capped at 36 edges, so external read-only clients cannot bypass the LightRAG graph budget even when they request a large reading window.
- Neo4j LightRAG neighborhood calls now use the same bounded optional-I/O policy as Milvus, with `AGENT_NEO4J_OPERATION_TIMEOUT_MILLIS`, `AGENT_NEO4J_FAILURE_COOLDOWN_SECONDS`, and `reader_agent_graph_recall_total{outcome="success|fallback|disabled"}`. The graph fault helper forces an approved character seed so a paused Neo4j run exercises the actual LightRAG query path rather than only the regular reading fallback.
## 方案 A 收尾更新：Reranker 核心路径已本地化（2026-08-01）

方案 A 已落地：`LocalEvidenceReranker` 是默认且无第三方依赖的核心排序器，融合有界语义分数、BM25 风格关键词、章节证据质量和多来源佐证；新增 50 次重复运行测试验证结果稳定、无重复、结果数量受限。`ConfiguredRerankerService` 只在显式配置时调用外部 OpenAI-compatible Reranker，所有未配置、超时、错误、冷却和畸形响应均回退到本地排序。

因此“缺少第三方 Reranker 凭据”不再属于产品未完成项。`validate-agent-external-readiness.ps1` 在无外部配置时返回 `local_ready_external_optional`，并暴露 `localReranker=true`；配置外部供应商后才额外执行探测。真实供应商排序和 50 请求压测仍可按手册作为可选增强证据。

新增 `scripts/validate-agent-local-reranker.ps1`，可在无密钥、无外部网络依赖的环境运行聚焦回归。LightRAG-only、36 条局部边预算和 `BOOK`/`BOOK_SAFE` 上下文禁入约束保持不变。

## 本轮体验问题修复（2026-08-10）

- 打卡事件由 `reader-novel` 消费后调用用户服务的内部每日任务接口，积分发放使用每日任务集合与积分流水请求号双重幂等；用户服务调用失败时保留消息重试，不再因提前写入消费幂等键而吞掉积分。
- 新增独立的“知识图谱管理”页面，按当前用户书架逐本读取带用户上下文的 LightRAG 覆盖范围，支持查看洞察、公开/私有切换和删除本人图谱。
- Agent 对话工作台统一为浅色纸张体系，修复会话列表、模型和洞察区域的深色块与低对比文字。
- 推荐请求继续强制经过平台可读书源搜索；服务端会保留已验证候选，并在模型遗漏正文引用时追加“平台书源引用”区块，前端同步渲染可点击作品卡片。
- 验证：`reader-checkin` 集成测试、`reader-agent` 测试和 `frontend` 生产构建均通过。
