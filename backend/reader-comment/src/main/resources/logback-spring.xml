<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- 从 Spring 环境获取应用名 -->
  <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="reader"/>

  <!-- ─── 控制台（保持原有行为） ─── -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ─── 错误文件（仅写入 ERROR 级别） ─── -->
  <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>logs/error.log</file>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>ERROR</level>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>logs/error.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>7</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [${APP_NAME}] [%thread] ERROR %logger{60} - %msg%n%ex{full}</pattern>
    </encoder>
  </appender>

  <!-- ─── 根日志 ─── -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ERROR_FILE"/>
  </root>

  <!-- 项目包输出 DEBUG -->
  <logger name="com.shanyuefang" level="DEBUG" additivity="false">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ERROR_FILE"/>
  </logger>

</configuration>
