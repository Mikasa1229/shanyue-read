spring:
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
  config:
    import: ""
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
